{
  "measured": "2026-09-17",
  "note": "Open CodeQL alerts on main. A ratchet in BOTH directions: a rise fails, and so does a fall, because a baseline nobody lowers stops describing the tree. Lower it in the commit that fixes or dismisses the alert. Every count here is untriaged work, not an accepted level. One thing this gate cannot see, which is worth carrying between readings: it compares severity BUCKETS, so alerts leaving and alerts arriving in the same bucket net off against each other and an arrival is reported as no rise at all \u2014 a falling total is not evidence that nothing was introduced. The By rule table in the run summary is the only place an arrival shows, and nothing gates on it, so read arrivals out of `predicted` below rather than out of the total.",
  "predicted": "Nothing is predicted here: main MEASURED 5 high on 2026-09-17 (js/polynomial-redos 4, js/path-injection 1), read from `gh api --paginate 'repos/akasecurity/ai-tc/code-scanning/alerts?state=open&per_page=100'` \u2014 the gate's own call \u2014 rather than reasoned about. The 23 to 5 is nineteen alerts leaving and one ARRIVING, which the severity ratchet nets into a plain fall. Each was taken from the API's own state, dismissed_reason and fixed_at fields. LEFT (19): js/weak-cryptographic-algorithm #19 (packages/persistence/src/repositories/installed-packs.ts) FIXED 2026-08-20T18:05:57Z, hours after the 23 was read, which is why every scheduled run from 2026-08-21 until the dismissal sweep below reported 22 and not 23 \u2014 this baseline was stale the day after it was written; sixteen js/path-injection DISMISSED 2026-09-10T11:25:40Z to 11:26:02Z, every one `won't fix` and every one carrying a recorded comment, which is one judgement applied sixteen times in twenty-two seconds rather than sixteen triage decisions \u2014 the scan target is a local path the machine's own user names, which is the feature rather than a taint; two js/polynomial-redos #21 #22 (packages/detections/src/egress/extract.ts and packages/detections/src/egress/manifests.ts) FIXED 2026-09-11T14:09:44Z. ARRIVED (1): js/path-injection #50 at packages/local-ops/src/fs-scan.ts:292, on the statSync of the scan target that has been there since 2026-07-24, first reported 2026-09-11T07:12:32Z, which is after the dismissal sweep, so it was never in its scope. Which commit raised it is NOT attributed here: four commits touched that file on 2026-09-09 and 2026-09-10, the hardening cluster that added canonicalization to defeat symlink aliasing, and the alerts API keeps only the current instance, so nothing read in this session says which of the four moved the dataflow. It is UNTRIAGED and this baseline does not accept it: it reads as the same by-design pattern those sixteen cite, in the very file four of them came from, but nobody has ruled on it. The remaining four are js/polynomial-redos, untriaged and unmoved since the last reading: #20 packages/dashboard-ui/src/data-shares/atoms.tsx, #25 packages/plugin-sdk/src/config-inventory.ts, #31 packages/setup-wizard/src/triage/parse-verdict.ts, #44 packages/persistence/src/ids.ts.",
  "total": 5,
  "bySeverity": {
    "high": 5
  }
}
