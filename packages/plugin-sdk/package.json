{
  "name": "@akasecurity/plugin-sdk",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./scan-worker": "./src/scan-worker.ts",
    "./bare-command": "./src/bare-command.ts",
    "./browser": "./src/mask.ts"
  },
  "scripts": {
    "gen:bundled-packs": "node scripts/gen-bundled-packs.mjs",
    "lint": "eslint src test bench *.config.* && eslint --no-config-lookup -c eslint.scripts.config.mjs scripts",
    "test": "vitest run",
    "bench": "vitest bench --run --outputJson=bench-results.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@akasecurity/detections": "workspace:*",
    "@akasecurity/persistence": "workspace:*",
    "@akasecurity/schema": "workspace:*",
    "ignore": "^7.0.6",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@akasecurity/eslint-config": "workspace:*",
    "@types/node": "^24.13.3",
    "eslint": "^9.0.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.10"
  }
}
