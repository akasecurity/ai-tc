{
  "measured": "2026-09-17",
  "note": "Open CodeQL alerts on main. A ratchet in BOTH directions: a rise fails, and so does a fall, because a baseline nobody lowers stops describing the tree. Lower it in the commit that fixes or dismisses the alert. Every count here is untriaged work, not an accepted level. Two things this gate cannot see, both worth carrying between readings. It compares severity BUCKETS, so alerts leaving and alerts arriving in the same bucket net off against each other and an arrival is reported as no rise at all \u2014 a falling total is not evidence that nothing was introduced. And a dismissal binds to an alert NUMBER rather than to a finding: when a code change moves a finding, the analysis can close the old number and mint a new one, which drops the dismissal and puts an already-ruled-on finding back into this count as though it were new work. Before treating a number as new, check whether it is an older one relocated \u2014 same rule, same file, and an old fixed_at equal to the new created_at. The By rule table in the run summary is the only place either shows, and nothing gates on it.",
  "predicted": "Nothing is predicted here: main MEASURED 5 high on 2026-09-17 (js/polynomial-redos 4, js/path-injection 1), read from `gh api --paginate 'repos/akasecurity/ai-tc/code-scanning/alerts?state=open&per_page=100'` \u2014 the gate's own call \u2014 rather than reasoned about. The 23 to 5 is nineteen alerts leaving and one coming BACK under a new number, which the severity ratchet nets into a plain fall. Each disposition below was taken from the API's own state, dismissed_reason, dismissed_at and fixed_at fields. LEFT (19): js/weak-cryptographic-algorithm #19 (packages/persistence/src/repositories/installed-packs.ts) FIXED 2026-08-20T18:05:57Z, hours after the 23 was read, which is why every scheduled run from 2026-08-21 until the dismissal sweep below reported 22 and not 23 \u2014 this baseline was stale the day after it was written; sixteen js/path-injection DISMISSED 2026-09-10T11:25:40Z to 11:26:02Z, every one `won't fix` and every one carrying a recorded comment, which is one judgement applied sixteen times in twenty-two seconds rather than sixteen triage decisions \u2014 the scan target is a local path the machine's own user names, which is the feature rather than a taint; two js/polynomial-redos #21 #22 (packages/detections/src/egress/extract.ts and packages/detections/src/egress/manifests.ts) FIXED 2026-09-11T14:09:44Z. BACK UNDER A NEW NUMBER (1): js/path-injection #50 is #5 RELOCATED. It is not a new finding and not one the sweep never scoped. #5 was dismissed `won't fix` in that same sweep at 2026-09-10T11:25:43Z, and its fixed_at is the same second as #50's created_at, 2026-09-11T07:12:32Z, on the same rule, the same file and the same five-line message. Both sit on one sink, the statSync of the scan target inside collectFiles: line 102 at f995dad7, the last main analysis carrying #5, and line 292 now, after the canonicalization inserted above it moved the location fingerprint. The analysis that renumbered it is the 2026-09-11T07:12:32Z run on d2cb9f63, the merge carrying the 2026-09-09 and 2026-09-10 hardening cluster to main. So the dataflow did not change, its LINE did, and the dismissal did not follow the renumbering. It is counted because it is genuinely open, but it is NOT untriaged the way the other four are: it was ruled on once, as #5. Re-dismissing it with #5's comment would take this baseline to 4; that is a maintainer call and is deliberately not taken here. The remaining four are js/polynomial-redos, untriaged and unmoved since the last reading: #20 packages/dashboard-ui/src/data-shares/atoms.tsx, #25 packages/plugin-sdk/src/config-inventory.ts, #31 packages/setup-wizard/src/triage/parse-verdict.ts, #44 packages/persistence/src/ids.ts.",
  "total": 5,
  "bySeverity": {
    "high": 5
  }
}
