{
  "name": "@akasecurity/detections",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src test bench *.config.*",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "bench": "vitest bench --run --outputJson=bench-results.json"
  },
  "dependencies": {
    "@akasecurity/schema": "workspace:*"
  },
  "devDependencies": {
    "@akasecurity/eslint-config": "workspace:*",
    "@akasecurity/extract": "workspace:*",
    "@types/node": "^25.9.5",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^9.39.5",
    "typescript": "^5.8.0",
    "vitest": "^4.1.10"
  }
}
