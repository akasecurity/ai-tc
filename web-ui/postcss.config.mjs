// Tailwind v4 via PostCSS (Next has no Vite plugin; the dashboard uses
// @tailwindcss/vite, the web-ui uses @tailwindcss/postcss).
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
